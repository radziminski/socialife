import 'package:flutter/material.dart';
import 'package:socialife/widgets/layout/page_wrapper.dart';

class OrganizationEventsPage extends StatelessWidget {
  const OrganizationEventsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const PageWrapper(
      child: Center(
        child: Text(
          'OrganizationEventsPage - organization page for events management - TODO',
        ),
      ),
    );
  }
}
