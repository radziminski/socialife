import 'package:flutter/material.dart';
import 'package:socialife/widgets/layout/page_wrapper.dart';

class UpdateTicketTypePage extends StatelessWidget {
  const UpdateTicketTypePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const PageWrapper(
      child: Center(
        child: Text(
          'UpdateTicketTypePage - creating ticket type for event - TODO',
        ),
      ),
    );
  }
}
